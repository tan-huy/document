// Animate Smooth Scroll
$("#view-document").on("click", function() {
  const images = $("#main-doc").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});

$("#view-A").on("click", function() {
  const images = $("#A").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});

$("#view-B").on("click", function() {
  const images = $("#B").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});

$("#view-C").on("click", function() {
  const images = $("#C").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});

$("#view-D").on("click", function() {
  const images = $("#D").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});

$("#view-E").on("click", function() {
  const images = $("#E").position().top;

  $("html, body").animate(
    {
      scrollTop: images
    },
    900
  );
});